const path = require('path');
module.exports = {
	entry: {
		'/dist/assets/js/main': './src/assets/js/main.js',
		'/dist/assets/js/slider': './src/assets/js/slider.js',
	},
	module: {
		rules: [
			{
				test: /\.(js)$/,
				exclude: /node_modules/,
				use: ['babel-loader']
			}
		]
	},
	resolve: {
		extensions: ['*', '.js']
	},
	mode: 'none',
	output: {
		filename: '[name].min.js',
		path: path.resolve(__dirname, '.')
	},
	optimization: {
		minimize: true
	}
};